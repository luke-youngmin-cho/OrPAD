# OrPAD Workspace Guidelines

Guidelines are local instructions for agents and generated harnesses.

Use this folder for workspace-level guidance that applies across pipelines, such as:

- Harness generation expectations.
- Local verification policy.
- Workspace-specific safety rules.
- Product or content quality contracts.

Pipeline-specific rules should still live under each pipeline's `rules/` folder.
